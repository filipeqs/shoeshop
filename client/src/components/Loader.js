import React from 'react';

const Loader = () => {
    return (
        <div className="loader-container">
            <img className="loader__img" src="/images/loading.svg" alt="loader" />
        </div>
    );
};

export default Loader;
